# Math Error Detection Datasets - Setup Requirements
# Install with: pip install -r requirements_datasets.txt

# Core dataset handling
datasets>=2.0.0
requests>=2.25.0

# Data processing and analysis  
pandas>=1.3.0
numpy>=1.20.0

# Image handling (for FERMAT and EGE datasets)
Pillow>=8.0.0

# Visualization (optional, for data exploration)
matplotlib>=3.3.0

# JSON handling and file operations (usually built-in, but ensuring compatibility)
pathlib2>=2.3.0; python_version<"3.4"

# Progress bars (optional, for better user experience)
tqdm>=4.60.0

# HuggingFace datasets specific
huggingface-hub>=0.10.0